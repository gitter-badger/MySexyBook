<main id="page">
	<div class="container">
		<h1>Bienvenue sur le book de {{= it.user.pseudo }}</h1>

		<p>Date d'inscription : {{= it.strftime("%d %B %Y", it.user.register_date) }}</p>
		
		{{? it.books.length }}
			<ul>
			{{~ it.books :book }}
				<li><a href="{{= it.app.url}}/book/{{= it.user.pseudo}}/{{= book._id}}">{{= book.title }}</a></li>
			{{~}}
			</ul>
		{{??}}
			<p><em>{{= it.user.pseudo }}</em> n'a pas ajouté encore de photos</p>
		{{?}}
	</div>
</main>